import { PercentComputePipe } from './percent-compute.pipe';

describe('PercentComputePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentComputePipe();
    expect(pipe).toBeTruthy();
  });
});
